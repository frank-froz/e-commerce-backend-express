# 🔐 Variables de Entorno - E-commerce Backend
# ⚠️ Copiar este archivo como .env y configurar con tus valores

# 🗄️ Base de Datos PostgreSQL (Supabase o local)
DATABASE_URL="postgresql://usuario:password@host:5432/database"

# 🔄 Prisma - Conexión Directa (para migraciones)
DIRECT_URL="postgresql://usuario:password@host:6543/database"

# 🌐 Servidor
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

# 🔑 JWT Secrets (⚠️ CAMBIAR EN PRODUCCIÓN)
# Generar con: openssl rand -base64 64
JWT_SECRET=tu-clave-secreta-super-segura-cambiar-en-produccion
JWT_EXPIRES_IN=30m
JWT_REFRESH_SECRET=otra-clave-diferente-para-refresh-token-segura
JWT_REFRESH_EXPIRES_IN=7d

# 🔐 Autenticación
BCRYPT_SALT_ROUNDS=10

# 🍪 Cookies (cambiar a true en producción con HTTPS)
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
